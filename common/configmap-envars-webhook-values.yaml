---
apiVersion: v1
kind: ConfigMap
metadata:
  name: envars-webhook-values
  namespace: ${projectNamespace}
  labels:
    "kommander.d2iq.io/application": "envars-webhook"
data:
  values: |
    webhook:
      namespaceSelector: ${projectNamespace}
      verboseLogs: false 
      containersAllowed:
        ingester: true
        prober: true
        store-gateway: true
